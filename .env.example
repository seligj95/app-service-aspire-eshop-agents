# Azure AI Foundry Configuration
# REQUIRED: Get these values from your Azure AI Foundry project
# Go to https://ai.azure.com → Your Project → Project Settings → General
AI_PROJECT_ENDPOINT=https://your-project-name.eastus.api.azureml.ms
AI_MODEL_DEPLOYMENT_NAME=gpt-4.1

# External Inventory Service Configuration  
# REQUIRED: Base URL to your external inventory service
# The system will automatically append endpoints:
# - /api/inventory (for REST API inventory queries)
# - /mcp (for MCP server endpoint used by agents)
EXTERNAL_INVENTORY_URL=https://your-inventory-service.azurewebsites.net

# Local Development Overrides (optional)
ASPNETCORE_ENVIRONMENT=Development